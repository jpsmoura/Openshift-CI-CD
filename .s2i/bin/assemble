#!/bin/sh

echo "Joao Test: " $APP_LOCATION

curl -H "Accept: application/zip" $APP_LOCATION -o ~/${BUNDLE_NAME}
echo $PWD

# Unzip and remove the root directory
tar -xvzf ${BUNDLE_NAME} -C ~/hawt-app --strip 1
cd ~
echo "Joao Files " ls -l | grep "^d"
